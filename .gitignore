*.json
*.log
nohup.out
.env
__pycache__/
*.pyc
.primary_updating
*.txt
!requirements.txt
*.zip

# docs/ 配下はすべて非公開（公開不要・push しない）
#   docs/ai = AI 引き継ぎ用の内部資料（SSH/VM/旧パスワード等の内部情報を含む）
#   docs/me = 本人用の出力・検証ファイル置き場
docs/
# 評価の候補プール（再生成可能な生成物）
eval_pool_*.csv
# バックアップ・一時ファイル（マージ作業の中間生成物・再生成可能）
*.bak
# 日付サフィックス付きの退避（*.bak では末尾一致しないため別途。2026-07-26 に
# .env.bak_20260726（AWS 鍵と DB パスワードを含む）がすり抜けていたのを発見）
.env.bak*
*.bak_*
*.tmp
*.premerge_bak
*.rank_backup
# Wisdom Guild の HTML スクレイプキャッシュ（再取得可能・サイズ大）
wg_cache/
# CLAUDE.md は Claude Code のプロジェクト設定（呼称・課金規律等の内輪情報を含む）
# ＝リポジトリは公開ポートフォリオなので push しない
CLAUDE.md

# 旧 Chat(Sonnet) 日報。docs/ai に役割を引き継いだため不要・push しない
Chat_log/

# 役目を終えた一回きりスクリプト・重複・旧版（2026-07-06 にリポジトリから除外。ローカルには残置）
add_face_cmcs.py
add_face_names.py
add_face_names_wiki.py
add_new_cards.py
backfill_japanese_full.py
compare_bulk.py
csv/eval_groundtruth.csv
env.example
fix_card_data.py
fix_ja_slots.py
fix_layout.py
ingest_marvel_en.py
merge_gt.py
merge_gt_simple.py
restore_missing_50.py
scrape_wisdom_guild.py
sh/merge_gt.sh
sql/fix_face_names.sql
# 2026-07-06 の構造化置換で死んだ手書き規則モジュール（役割判定は target_types/removal 列へ移行）
mtg_removal_rules.py
mtg_counter_rules.py
# 一回きりの GT マージスクリプト（採点ラウンドごとに使い捨て）
merge_gt_*.py
# 一回きりの分析・確認スクリプト（出力は docs/me に保存済み・採点ラウンドごとの使い捨て）
analyze_90s_20260706.py
check_pool_*.py

# 採点マージ等のバックアップ・作業中間ファイル（push しない・status ノイズ対策）
*.premerge_bak_*
*.prer11_bak_*
*.pregrade_bak
regrade_keyword_sweep_*.csv
WHISPER不一致.md
md/

# 採点ワークシート（採点結果は GT に union 済み＝中間生成物・push しない）
removal_direct_pool_*.csv
removal_regrade_pool_*.csv
removal_*pool* - *.csv
# GT のマージ前バックアップ（世代別・ローカルのみ）
*.premachine_bak_*
*.preowner_sweep_bak_*
counter_kakutei_pool_*.csv
counter_direct_proto_*.py
# 役目を終えた一回きり（2026-07-24 に追跡から退出・ローカル残置・履歴には残る）
arm_diag_20260715.py
backfill_tournament_names_20260718.py
condition_vacuity_20260715.py
counter_kakutei_pool_20260718.py
fix_ja_face_dupes_20260715.py
modern_removal_quality_audit_20260715.py
removal_direct_proto_20260715.py
removal_regrade_pool_20260717.py

# Terraform（2026-07-26）: 作業ディレクトリと state は push しない。
#   .terraform/      プロバイダのバイナリ（数百 MB）
#   *.tfstate*       **実インフラの現状＝機微情報を含む**（DB エンドポイント等）
#   *.tfvars         実値（アカウント ID 等）。*.tfvars.example は追跡する
infra/.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
crash.log

# Aurora 搬入用の一時 CSV・DDL（2026-07-28・搬入後に消してよい）
migration/

# 本人と共用する道具の置き場（2026-07-28）。バイナリと内輪用スクリプトは追跡しない。
#   bin/terraform            112MB の実行ファイル（GitHub の 100MB 上限にも当たる）
#   bin/fable_chat_mirror.py 相談役サブエージェントの会話ミラー＝内輪の道具
# bin/dbq のような公開して困らない小道具は追跡を続ける
bin/terraform
bin/fable_chat_mirror.py

# ルール原文（WotC 配布物・リポジトリに含めない）
data/rules/
