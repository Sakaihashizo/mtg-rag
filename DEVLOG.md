# DEVLOG — 開発記録（時系列）

週次の開発記録。[README](./README.md) は「現在地のスナップショット」に保ち、**時系列の物語と週ごとの変更はこのファイルに追記する**。個々のトピックの詳細な技術解説は、今後は技術ブログ記事として切り出す予定（記事化したらここからリンクする）。

設計詳細は [ARCHITECTURE.md](./ARCHITECTURE.md)、評価スコアの系譜は [EVAL_SCORES.md](./EVAL_SCORES.md)。

---

## 2026-07 第5週（07-27〜07-31）

**AWS デプロイ完走と、LLM の持ち分を実測で決めた週——動く URL の誕生・腕の切除実験・LLM ルーターの段階的降格。**

### 1. デプロイ — 一晩で工程表を完走し、受け入れ検収まで
Terraform 32 リソースを記述して plan 通過、apply からデータ搬入・Lambda コンテナ・CloudFront 配信までを一晩で完走した（apply とデータ搬入は開発者自身がクエリエディタから実行＝マネージド DB の運用を手で経験する判断）。検収はクラウド側 Aurora を検索対象にした評価の実走で行い、Data API 固有の罠を三つ潰した——jsonb が文字列で返る・配列パラメータ非対応・SET がセッションごとに揮発する（HNSW の探索設定が毎文消えてベクトル recall が崩れた。ALTER DATABASE で恒久化）。初月の請求実測は $1.48 で、設計時の「放置時 $1 前後」を裏づけた。

### 2. 腕の切除実験（ablation）— 埋め込みの給料を確定させた
「ベクトル検索は本当に働いているのか」という疑いに、腕を一本ずつ外して測ることで答えた。埋め込みを全廃すると本線 0.94 → 0.71。ただし壊れ方が二種類あることが分かった——辞書に載らない言い回しの受け皿が消える正味の損失と、**英語クエリの FTS 展開キーが不在で埋め込みが肩代わりしていた分**。後者は辞書の一括追補（生得キーワード 33 語・日本語辞書からの自動生成）で解消し、埋め込みの本当の担当領域が「開いた語彙の吸収」であることを特定した。記事化候補: 「LLM とベクトルが要らなかった境界線を実測で引く」。

### 3. LLM ルーターの段階的降格 — 申告者から生成器へ
LLM が自己申告する意図フラグ（除去・カウンターの mode）を廃止し原文の決定的検出へ、型の申告は searcher 側の関所で原文と突き合わせて裁く形へ。契機はどちらも実測事故（幻出したフラグ・型が検索結果を汚した）。ルーターの出力 9 項目のうち決定的に代替できないのは「言い換えの正規化」と「HyDE 文の生成」の二つだけ、という整理に到達した。検証の関所を経路ごとに貼らず一点に置く（貼り忘れた経路で必ず再演するため）。

### 4. サーチ列と濾過列 — 「位置を知っているか」で分ける
「土地をサーチするカード」が埋め込み頼みだった問題に、サーチ（ライブラリ全域に条件でアクセス）と濾過（上から N 枚の位置に縛られる）を**別の列**として導出して答えた。占術・諜報・切削して拾う型まで同じ器で表現する（見る枚数・取る枚数・残りの行き先・拾える型を効果ごとに束ねた配列）。「引くだけのカードは濾過でない」という採点の線も同時に制定。土地サーチ 1.000・「ドローしながらフィルタリング」0.60→0.91。

### 5. 評価トラックの三本立て
本線（構造化で解ける族・0.937）・EDH（0.963）に加え、**構造化で解けない実ユーザークエリだけの意味検索トラック（0.41）を別掲で新設**。看板と混ぜれば平均は上がるが混ぜない。低い数字を隠さず持つことが、次に何を作るべきかの地図になる。

## 2026-07 第4週（07-20〜07-26）

**ドロー族とマナ族の構造化、EDH 評価トラックの開始——NDCG@10 0.886 → 0.939。統率者戦は別掲で開始し、採点是正を経て 0.938。**

- **768 次元 embedding（BASE）を退役**: SMALL（384 次元）との同条件一対比較でベクトルが所有する全層が敗北。「次元を増やしても記述層は救えない——救うのは構造化列」を実測で確認し、アーカイブ退避の上で DROP。二重保守が終了した。〔記事化候補: "embedding の次元数より列"〕
- **統率者名→固有色ゲート**: 「アトラクサで使える除去」の統率者名を名前辞書（伝説のクリーチャー 3,125 枚・読点短縮名対応）で固有色の部分集合条件に解決。敵対文脈（「〜対策」等）は名前の近傍だけで判定する設計に、複合クエリの実測（「対応」の一語が正当な発動を殺す）から格上げ。非対称安全試験 22 ケース。
- **多人数 Commander データ開通**: Moxfield の非公式 API 利用許諾を取得し、ブラケット別に 1,232 デッキを取得。EDH 系の play-rate と共起を構築系テーブルから**物理分離**（100 枚シングルトンの共起は意味論が別物）。「多人数 Commander の実勢」が Duel Commander の近似から実データに切り替わった。
- **大会データの夜間バッチ稼働開始**: 毎日 03:00 に差分スクレイプ→採用率再集計。何度走っても結果が変わらない冪等設計・時刻の門つき。稼働初日は 35 パス全レーン正常・+776 デッキ。
- **ドロー族の構造化（採点規約 R14）**: 「引く」を oracle テキストの**命令文の「Draw N card(s)」の行為**として規約化——誘発の条件文・置換文に現れる draw は数えない（「破壊不能」が destroy の字面で除去に紛れる問題のドロー版）。導出列＋枚数ゲートを全腕へ配線し、**ゲート単体の寄与 +0.011 を同一 DB 状態の ON/OFF 対照走行で分離測定**（非ドロークエリの変動ゼロ＝誤発動ゼロの実証）。draw two cards **0.394 → 0.951**・「カードを2枚引く」「インスタントでドロー」は **1.000**。〔記事化候補: "対照走行でゲートの純効果を分離する"〕
- **採点債務の返済**: 明文化されていない「実用的でない＝減点」の軸で引かれた旧採点を発見し、機能ベースへ是正（規約 R14 補足として明文化）。素の英語クエリが語彙辞書に掛からず全文検索ゼロ件＝ベクトル一本腕で走っていた**日英非対称**も実測（`en_fts:0`）から発見・修理。看板 **0.932** で戴冠。
- **EDH 評価トラック開始**: 統率者名軸の 10 クエリ・人手採点 100 ペアを**本線と分母を混ぜず別掲**で開始。初値 **0.975** には「採点の網が浅い初期値は高く出る」という逆向きのバイアスを明記——実態をよく映すのは複合クエリの precision@5 **0.4** の方で、脅威（破壊不能）と除去機構の整合判定が次の改善候補。
- **マナ族の構造化（採点規約 R15）**: マナ加速の判定列を「マナを出すか」から「**マナを出すことが本業か**」へ再定義（死亡時の宝物など、誘発の報酬として出るマナは数えない）。旧列にあった「`Add {U} or {B}`（どちらか 1 点）を 2 点と合算する」誤りで二色土地が数百枚まぎれていた不具合も同時に解消。**「列を直さずに並び順だけ採用率順へ変えると悪化する」ことを対照実験で確認**——誤判定の札ほど構築での採用率が高く、並べ替えが誤りを押し上げるため。順序は「列 → 並び順」。パイオニアのマナ加速 0.948→**1.000**、1マナのマナクリーチャー 0.703→**0.943**。〔記事化候補: "順序が結果を決める——列と並び順の依存関係"〕
- **再順位付けモデルの棚卸し**: 3 ヶ月測っていなかった cross-encoder を同一条件で再測定し、寄与が **+0.024 → +0.006** に縮んでいたことを確認（構造化列が仕事を吸収した結果）。同時に、**「定義として正しいか」は測れても「利用者にとって嬉しいか」は測れない**ことを実証——採用率順で並んでいた除去クエリに掛けると、そのフォーマットの代表的な札（Lightning Bolt・Fatal Push）が軒並み下がった。2.2GB のモデルを本番に積まない判断の実測根拠とした。〔記事化候補: "測って要らないと判断する"〕
- 週間の学び: **作者の素朴な違和感——「なぜ低い」「英語だと下がる？」「0.9 は低くないか」——が、採点債務・実装の非対称・評価の被覆の崖を連続で暴いた**。数字への違和感は放置せず、その場で対照実験に変換する。

## 2026-07 第3週（07-13〜07-19）

**採点基準の再定義と SQL 直行方式の本採用——NDCG@10 0.803 → 0.886。**

- **採点規約 R2' の制定と総再採点**: 機能クエリの 2/1 を「狙って恒久的に討てるか」で再定義し、除去系 300 ペア超を再採点。**0.803 → 0.865** の上げ幅の大半は検索の改善でなく**物差しの是正**（「利用者が求める順位」と理想順位の一致化）と明記して戴冠。非公式の有志和訳 831 枚を日本語列から除去して再 embedding（公式データ純度の回復）。
- **除去直行方式の本採用**: 人手採点で検証を完了した除去クエリ 9 本を「役割の構造化 WHERE ＋大会採用率順」の SQL 直行路へ移行（ファミリー平均 **0.987**）。発動は検証済みクエリの完全一致のみ・誤発動ゼロの安全試験 38 ケース。検証終了クエリは「床」として分母外で毎回測定する運用に（得意な問題を試験に足して点を盛らない）。
- **確定カウンターへの置換**: 「条件付きカウンター呪文」は実在の利用者が使う語でないと判断して退役し、「確定カウンター呪文」に置換。並びの規則は確定段→採用率で、除去のレシピとは共有しない——**「役割が違えばレシピが違う」**をレシピ相互適用の実測で確認。看板 **0.886**。
- **HyDE の適用範囲を実測で確定**: 「最強」系 5 クエリすべてで HyDE 停止が勝ち（大会実績が答えを持つ層では、意味の言い回しが実績信号を埋める）・機能記述系では HyDE が支柱——**クエリの種別で経路を選ぶ**設計の実測根拠。未採点を除外して測る併記指標（judged）も導入。
- 使用率のフォーマット別化・Vintage/Pauper の大会デッキ着地・決定的ゲートへの原文併給（ルーターの写し間違いで門が不発になる事故への対処）。
- 週間の学び: **数字が上がったときこそ「何が上がったか」を分解して語る**——検索の改善・採点の是正・被覆の変化は別の出来事で、混ぜて「+0.083 改善」と言った瞬間に評価の信用が死ぬ。

## 2026-07 第2週（07-06〜07-12・活動記録は 07-11〜07-12）

**AWS 構成の決定・ローカルサーバ稼働・決定的ゲートの大幅拡張・RRF の管轄確定。**

- **AWS 維持費の総ざらい → Data API 型に構成決定**: デプロイ前に「一切使わず放置した場合の月額」を公式料金表（Bulk Pricing API）から全構成分算出。旧構成案には罠が 2 つ埋まっていた——(1)RDS Proxy は auto-pause と非互換（公式明記）な上に Serverless v2 へは最低 8 ACU 分の時間課金（Proxy だけで月 $146）(2)Interface 型 VPC エンドポイントは稼働状態と無関係に毎時課金。放置時 $232/月 → **$0.84/月** の Data API 型（Lambda を VPC 外に置き、HTTPS + IAM で Aurora に入る）へ再設計した。〔記事化候補: "RDS Proxy と auto-pause は両立しない"〕
- **ローカル API サーバ + Web UI**: FastAPI + 静的 1 ファイル UI（ビルド工程ゼロ＝S3 配信前提の構成をローカルで検証）。検索経路バッジとレイテンシを UI に表示。systemd user unit で VM 起動時に自動起動・死活監視（kill 後 5 秒で自動復旧を実測）。
- **ローカル 7B をルーターとして実戦配備**: `ROUTER_BACKEND=ollama` で API サーバのルーターをローカル 7B（$0）に切替可能に。検証層（後述）はプロバイダ非依存なので同じ保護が掛かる。
- **DB アクセス層の切替設計**: SQL 実行 14 箇所を単一ドライバ層（psycopg2 ⇔ Aurora Data API）に集約。移行が検索品質に完全透過であることを eval 全指標一致で証明。
- **決定的ゲートを 5 系統へ拡張**（すべて「本線 eval の全指標一致」を回帰条件に導入）: 生得キーワード 33 語の肯定・否定（「速攻を持たないクリーチャー」——embedding は否定文を原理的に理解しない）／日本語カードタイプ語／P/T 列間関係（「パワーとタフネスが同じ」）／部族 65 種（「蟹」2/10 → 5/5）／カード名部分一致（「カード名にナヒリとつく」1/10 → 10/10）。〔記事化候補: "embedding は否定文を理解しない"〕
- **LLM 出力の検証層を 3 段に**: 数値幻覚ガード（既存）に加え、排他境界の ±1 補正（LLM は「9より小さい→max=8」に成功しつつ同一クエリの「7より大きい→min=7」で失敗する、と実測）と `type_filter` 幻出ガードを追加。〔記事化候補: "LLM の出力を信用しない検証層の設計"〕
- **RRF の管轄を腕別アブレーションで確定**: 「RRF という仕組み自体が欠陥では」という骨格への疑いを、判定基準を事前固定した 6 条件の実測で検証。結果——FTS 2 腕は曖昧な意味の層で +0.088 の相補寄与（英=キーワード拡張の受け皿・日=日本語定型句の受け皿・寄与はほぼ加法的）・均等重みは局所最適・答えが一意に決まる層に合議を使うと人気者バイアス（実測）。「欠陥」ではなく「管轄が一意に決まる層から正しく剥奪され、残った曖昧な意味の層で稼ぐ現役」と結論。
- **クエリ観測ログ（`query_log`）稼働**: 全クエリの経路・ルーター解析・レイテンシを記録。直行路 11ms vs ルーター経由 15,024ms のような実測が SQL 一発で取れる。辞書拡張の候補抽出（human-in-the-loop 語彙学習）の観測基盤。
- 週間の学び: **UI で遊ぶ → `query_log` が証拠を取る → 検証層/ゲートで塞ぐ**というループが確立した（この週のバグ・穴は全部、作者自身が UI で遊んでいて発見した）。

## それまでの歩み（ダイジェスト・2026-05-25 起点）

| 週 | 主な出来事 |
| --- | --- |
| 5月末 | プロジェクト開始。PostgreSQL + pgvector・日英 embed_text・ハイブリッド検索の骨格 |
| 6月上旬 | RRF 重みグリッドサーチ（均等重み採用）・HNSW ベンチマーク・LLM-as-Query-Router の 3 回試行・評価フレームワーク導入（決定的評価・ルーターキャッシュ） |
| 6月中旬 | Primary/Standby 検証構成（reembed 中の読み取り継続）・PostgreSQL チューニング・レプリケーション断の検出と復旧・観測性導入（pg_stat_statements 等） |
| 6月下旬 | GT 拡張の方法論確立（被覆バイアス対策・未ラベル混入率 0%）・日本語 HyDE / reranker / is_mana_boost の A/B（NDCG@10 0.574 → 0.673）・データ品質の系統修復（名寄せ 99.96%・両面カード日本語名補填・ソース照合監査） |
| 7月第1週 | 採点規約の明文化と GT 厳格化・大会 play-rate の接続（強度腕・機械採点）・役割ゲート・構造化オンリー直行路・カウンター条件性/除去機構ゲート——**正準 NDCG@10 0.811 に到達**・ローカル 7B ルーター検証（30/30）・EDH トラック開始（色ゲート・EDH 腕）・Bedrock Nova Micro 開通と調教（$0.00013/クエリ）・解説レイヤーのブラインド試飲会 |

7月第2週以降は、上の週次エントリで詳述している。

---

## 記事化候補（ブログ執筆キュー）

1. RDS Proxy と auto-pause は両立しない——Aurora Serverless v2 の放置時維持費を公式料金表から総ざらいした
2. embedding は否定文を理解しない——「速攻を持たないクリーチャー」を SQL の NOT で解く
3. LLM の出力を信用しない——数値幻覚・境界演算・型幻出の 3 段検証層
4. 個人開発の RAG に評価基盤を作る——NDCG 0.574 → 0.811 の系譜と「未ラベル混入率 0%」の方法論
5. RRF は欠陥か？——腕別アブレーションで「合議の管轄」を実測した
6. ローカル開発に Nginx が要らなかった理由——サーバの階層と玄関番の仕事
7. embedding の次元数より列——768 次元 BASE の退役（同条件一対比較で全敗の実測）
8. 対照走行でゲートの純効果を分離する——同一 DB 状態の ON/OFF A/B と「非対象クエリの変動ゼロ」という証明
